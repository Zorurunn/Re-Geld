import { useData } from "../layout";

export default function Balance() {
  // const { isDone, setIsDone } = useData();
  const { isHidden, setIsHidden } = useData();
  console.log(isHidden);
  return <div>sss</div>;
}
